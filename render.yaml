services:
  - type: web
    name: stock-dashboard
    env: python
    plan: free
    region: oregon
    buildCommand: |
      pip install flask flask-cors yfinance pandas numpy python-dotenv google-genai google-api-core 
    startCommand: python app.py

